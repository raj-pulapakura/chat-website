query AccountById($accountId: ID!) {
  accountById(accountId: $accountId) {
    account {
      id
      createdAt
      updatedAt
      username
      password
    }
    error {
      ...FieldErrorFragment
    }
  }
}
