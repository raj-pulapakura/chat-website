mutation JoinRoom($publicId: ID!, $userId: ID!) {
  joinRoom(publicId: $publicId, userId: $userId) {
    error {
      field
      message
      resp
    }
    userRoom {
      userId
      roomId
    }
  }
}
